def replace_email(email, old_domain, new_domain):
    if "@" + old_domain in email:
        index=email.index("@"+old_domain)
        new_email=
        word="apple orange & others"
        print(word.index("apple"))
